# Implementation Plan (Executed)

1. Inspect repository modules for backend/admin/collector media and ingestion paths.
2. Add DB structures for media library + usage mappings with backward compatibility.
3. Add backend APIs for media asset CRUD/review + usage attach/detach.
4. Add admin Media Library UI for listing/reviewing/editing metadata.
5. Add admin picker integration into place/event editing flows.
6. Add collector raw ingestion tables and modular source adapter pipeline.
7. Add collector API/UI for adapter-based raw collection only (no auto publish).
8. Validate builds/checks and document migration/testing/risk notes.
