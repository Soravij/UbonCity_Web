# Frontend Project Commands

```text
cd frontend
npm.cmd install
npm.cmd run dev
npm.cmd run build
npm.cmd run start
npm.cmd run lint
npm.cmd run test:phase56
curl.exe -s "https://test.uboncity.com/th/review/<id>"
curl.exe -s "https://test.uboncity.com/th/<category>/<slug>"
```
