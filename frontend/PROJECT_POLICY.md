# Frontend Project Policy

- Frontend is render-only.
- Public pages must use backend/public API fields.
- Do not fix backend or media bugs by rewriting collector URLs.
- Public render must not depend on `collector-test`.
- Published content should be backend-controlled only.
